/**
 * Carepack / CarePulse entry (placeholder until app code lands).
 */
function main() {
  console.log(
    "CarePulse — see README.md, docs/architecture/REPO_STRUCTURE.md, and CarePulse_Healthcare_Blueprint_v1.pdf"
  );
}

main();
