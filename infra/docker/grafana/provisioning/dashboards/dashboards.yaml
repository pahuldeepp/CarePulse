apiVersion: 1

providers:
  - name: CarePulse
    orgId: 1
    folder: CarePulse
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
