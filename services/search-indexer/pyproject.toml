[project]
name = "search-indexer"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "aiokafka>=0.11.0",
    "opensearch-py>=2.6.0",
    "pydantic>=2.7.0",
    "structlog>=24.2.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
